# ======================================
# AUTHENTICATION
# ======================================
AUTH_SECRET=your_nextauth_secret_here_generate_with_openssl_rand_base64_32
AUTH_GOOGLE_ID=your_google_oauth_client_id
AUTH_GOOGLE_SECRET=your_google_oauth_client_secret

# ======================================
# DATABASE (PostgreSQL)
# ======================================
# ⚡ DATABASE_URL usa el POOLER (más rápido, para queries)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require&pgbouncer=true&connect_timeout=10&pool_timeout=10

# ⚡ DIRECT_DATABASE_URL usa conexión directa (para migraciones)
DIRECT_DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# ======================================
# STRIPE PAYMENTS
# ======================================
STRIPE_API_KEY=sk_test_your_stripe_test_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ======================================
# AI SERVICES
# ======================================
GEMINI_API_KEY=your_gemini_api_key
NEXT_PUBLIC_TENOR_API_KEY=your_tenor_gif_api_key

# ======================================
# FILE UPLOADS
# ======================================
NEXT_PUBLIC_UPLOADCARE_PUBLIC_KEY=your_uploadcare_public_key

# ======================================
# APPLICATION
# ======================================
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ======================================
# REDIS (Optional - for caching)
# ======================================
UPSTASH_REDIS_REST_URL=your_upstash_redis_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_token

# ======================================
# PUSHER (Real-time)
# ======================================
PUSHER_APP_ID=your_pusher_app_id
PUSHER_KEY=your_pusher_key
PUSHER_SECRET=your_pusher_secret
PUSHER_CLUSTER=your_pusher_cluster

# Cliente (públicas)
NEXT_PUBLIC_PUSHER_KEY=your_pusher_key
NEXT_PUBLIC_PUSHER_CLUSTER=your_pusher_cluster

